import React from 'react';

const ServiceBox = ({title, url}) => (
    <div className="column serviceBox">
        <span className="service-title">
            {title}
        </span>

    </div>
);

export default ServiceBox;